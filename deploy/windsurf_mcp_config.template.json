{
  "_comment": "Windsurf MCP config — copy to ~/.codeium/windsurf/mcp_config.json. Replace REPLACE_WITH_PATH with the absolute path to your sassymcp.exe.",
  "mcpServers": {
    "sassymcp": {
      "command": "REPLACE_WITH_PATH\\sassymcp.exe",
      "args": [],
      "env": {
        "SASSYMCP_LOAD_ALL": "1"
      }
    }
  }
}
