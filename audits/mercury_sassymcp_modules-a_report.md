<!--
   Copyright (c) 2026 Shane Smith / Sassy Consulting LLC. All rights reserved.
   Proprietary source. This notice is Copyright Management Information (17 U.S.C. 1202); removal or alteration prohibited.
   CodeMark: SCLLC1-SassyMCP-DG3NQEANUU62
-->
```json
{"severity":"critical","category":"SECURITY","file":"sassymcp/modules/audit.py","symbol":"log_tool_call","issue":"Sensitive arguments are logged without redaction, potentially leaking secrets","why":"Logs are written to $SASSYMCP_HOME/audit.log and audit.jsonl; if a tool receives passwords, tokens, or private keys they are stored in plaintext and could be read by any user with read access","fix":"Add a secret‑masking step before logging (e.g. regex‑based detection of common secret patterns) or allow callers to flag sensitive fields; truncate or replace such values with placeholders","confidence":0.95}
{"severity":"critical","category":"CONCURRENCY","file":"sassymcp/modules/_confirm.py","symbol":"_PENDING","issue":"In‑memory token table accessed without any synchronization","why":"Multiple async tool calls could read/write _PENDING concurrently leading to lost tokens, duplicate consumptions or race‑condition‑based token reuse","fix":"Guard all accesses to _PENDING with an asyncio.Lock (or use a thread‑safe dict such as collections.OrderedDict wrapped by a lock) and make make_purge_expired, make_token, consume_token acquire the lock","confidence":0.9}
{"severity":"critical","category":"SECURITY","file":"sassymcp/modules/linux.py","symbol":"_ssh_exec_stream","issue":"SSH password is passed via the command‑line -pw flag","why":"On Windows the password appears in the process command line and can be read by any user with access to the process list (Task Manager, wmic, ETW) exposing credentials","fix":"Switch to key‑based authentication (use PLINK_KEY_FILE) or feed the password via stdin (plink -pw -) and avoid exposing it in the argument list","confidence":0.95}
{"severity":"high","category":"SECURITY","file":"sassymcp/modules/_github_client.py","symbol":"request","issue":"json_body parameter is ignored; payload is never sent as JSON","why":"httpx.AsyncClient.request expects the keyword `json`, so all GitHub API calls that rely on a request body (e.g. push_files_atomic) send an empty body, causing malformed requests and possible unintended side‑effects","fix":"Rename the parameter to `json` and pass it to `client.request` as `json=json_body` (or simply forward `json_body` as `json`) and update all callers accordingly","confidence":0.9}
{"severity":"high","category":"CORRECTNESS","file":"sassymcp/modules/combos.py","symbol":"sassy_combo_codebase_grep","issue":"Calls undefined function `_search_files`","why":"When the tool is invoked it will raise AttributeError, breaking the combo workflow and preventing the user from performing a codebase grep","fix":"Import the correct function from fileops (e.g. `from .fileops import sassy_search_files as _search_files`) or call the registered tool via server reference","confidence":0.95}
{"severity":"medium","category":"SECURITY","file":"sassymcp/modules/fileops.py","symbol":"sassy_write_file","issue":"Path validation allows any path when `allowedDirectories` is empty","why":"An adversarial LLM could write to arbitrary files (e.g. system binaries, credential stores) if the user has not configured allowedDirectories, violating the principle of least privilege","fix":"Enforce a default safe directory (e.g. $HOME/.sassymcp/files) when no config is present, or reject absolute paths that are not under the MCP source tree or a user‑specified sandbox","confidence":0.85}
{"severity":"medium","category":"CONCURRENCY","file":"sassymcp/modules/_tool_loader.py","symbol":"get_tracker","issue":"Singleton `ToolUsageTracker` created without synchronization","why":"If multiple coroutines call `get_tracker` simultaneously during server startup they could each instantiate a separate tracker, causing duplicate state and inconsistent usage statistics","fix":"Guard creation with an asyncio.Lock or use double‑checked locking pattern to ensure only one instance is created","confidence":0.8}
{"severity":"medium","category":"SECURITY","file":"sassymcp/modules/crosslink.py","symbol":"_post_message","issue":"No validation or size limiting on the `payload` argument","why":"A malicious client could send arbitrarily large or malformed JSON that later gets deserialized or logged, leading to DoS or injection if downstream code evaluates the payload","fix":"Enforce a maximum payload size (e.g. 1 MiB) and validate against a JSON schema before inserting into the DB; reject or truncate non‑JSON content","confidence":0.85}
{"severity":"medium","category":"CONCURRENCY","file":"sassymcp/modules/_rate_limiter.py","symbol":"configure_group","issue":"Stores semaphore config as an int and later replaces it with a semaphore without locking","why":"If `configure_group` is called concurrently for the same group, two coroutines may both replace the int with different semaphore instances, breaking the per‑group concurrency limit","fix":"Separate configuration (max_concurrent) from the semaphore instance in distinct dicts and protect updates with an asyncio.Lock","confidence":0.8}
```